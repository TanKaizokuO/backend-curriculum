services:
  db-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: testpassword
      POSTGRES_DB: testdb
    ports:
      - "5432:5432"
  redis-test:
    image: redis:7-alpine
    ports:
      - "6379:6379"
